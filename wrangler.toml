name = "durable-objects-template"
main = "src/index.mjs"
compatibility_date = "2025-01-01"

[[durable_objects.bindings]]
name = "MAILBOX_DO" # 绑定名不变，代码不用改这里
class_name = "MailboxDOV2" # 👈 改成 V2

[[migrations]]
tag = "v20" # 👈 直接跳到 v20，确保全新
new_sqlite_classes = ["MailboxDOV2"] # 👈 使用新名字
