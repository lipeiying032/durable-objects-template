name = "durable-objects-template"
main = "src/index.mjs"
compatibility_date = "2025-01-01"

[[durable_objects.bindings]]
name = "COUNTER"
class_name = "MailboxDO"

[[migrations]]
tag = "v3" # 🚨 改成 v3，确保触发新迁移
new_sqlite_classes = ["MailboxDO"] # 🚨 关键：把 new_classes 改成 new_sqlite_classes
