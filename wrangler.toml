name = "durable-objects-template"
main = "src/index.mjs"
compatibility_date = "2025-01-01"

[[durable_objects.bindings]]
name = "MAILBOX_DO"   # 这个不要动
class_name = "MailboxDOV2"  # 👈 必须改！

[[migrations]]
tag = "v100"  # 👈 跳个大号，防止和旧记录撞车
new_sqlite_classes = ["MailboxDOV2"]  # 👈 必须改！
